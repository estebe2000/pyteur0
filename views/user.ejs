<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Espace Utilisateur</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Portail</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (role === 'professeur' || role === 'admin') { %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            GED
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/courses?role=professeur&ownerId=2&username=<%= username %>">Cours</a></li>
            <li><a class="dropdown-item disabled" href="#">Exercices (à venir)</a></li>
            <li><a class="dropdown-item disabled" href="#">QCM (à venir)</a></li>
          </ul>
        </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/eleve-dashboard">Terminal</a>
        </li>
      </ul>
      <span class="navbar-text text-white me-3">
        Connecté : <%= username %>
      </span>
      <a href="/" class="btn btn-outline-light">Déconnexion</a>
    </div>
  </div>
</nav>

  <div class="container py-4">
<% let logoPath; %>
<% if (niveau.includes('2nde') || niveau.includes('1ère') || niveau.includes('Terminale')) { %>
  <% logoPath = '/medias/logo.jpg'; %>
<% } else { %>
  <% logoPath = '/medias/pyteur.jpg'; %>
<% } %>

<img src="<%= logoPath %>" alt="Logo" class="mb-4" style="max-height:100px;">

<h1 class="mb-4">Bienvenue dans votre espace personnel (<%= username %>)</h1>
    <p>Contenu personnalisé pour l'utilisateur connecté.</p>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
